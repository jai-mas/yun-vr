@charset "UTF-8";
/**
 * 这里是uni-app内置的常用样式变量
 *
 * uni-app 官方扩展插件及插件市场（https://ext.dcloud.net.cn）上很多三方插件均使用了这些样式变量
 * 如果你是插件开发者，建议你使用scss预处理，并在插件代码中直接使用这些变量（无需 import 这个文件），方便用户通过搭积木的方式开发整体风格一致的App
 *
 */
/**
 * 如果你是App开发者（插件使用者），你可以通过修改这些变量来定制自己的插件主题，实现自定义主题功能
 *
 * 如果你的项目同样使用了scss预处理，你也可以直接在你的 scss 代码中使用如下变量，同时无需 import 这个文件
 */
/* uni.scss */
/* 颜色变量 */
/* 行为相关颜色 */
/* 文字基本颜色 */
/* 背景颜色 */
/* 边框颜色 */
/* 尺寸变量 */
/* 文字尺寸 */
/* 图片尺寸 */
/* Border Radius */
/* 水平间距 */
/* 垂直间距 */
/* 透明度 */
/* 文章场景相关 */
.museum-container {
  position: relative;
  height: 100vh;
  /* 热门博物馆标题 */
  /* 轮播图样式 - 修复显示不全问题 */
  /* 更多博物馆标题 */
  /* 博物馆列表样式 - 调整边距和预约按钮 */
}
.museum-container .bg-image {
  position: absolute;
  /* 改为absolute */
  top: -18rpx;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  /* 调整为0 */
}
.museum-container .header-bar {
  top: 70rpx;
  position: relative;
  margin-bottom: 20rpx;
  font-size: 43rpx;
  font-weight: bold;
  color: #000000;
  /* 改为白色文字 */
  z-index: 1;
  border-radius: 10rpx;
}
.museum-container .header-bar image {
  width: 32rpx;
  height: 32rpx;
  margin-right: 220rpx;
}
.museum-container .hot-title {
  top: 14px;
  position: relative;
  font-size: 36rpx;
  font-weight: bold;
  color: #000000;
  z-index: 1;
  margin: 20rpx;
  margin-top: 40rpx;
  margin-bottom: 20rpx;
  border-radius: 10rpx;
}
.museum-container .hot-museum-swiper {
  height: 350rpx;
  width: 100%;
  margin-top: 30rpx;
  box-sizing: border-box;
}
.museum-container .hot-museum-swiper .swiper-item {
  height: 100%;
  width: 100%;
  position: relative;
}
.museum-container .hot-museum-swiper .swiper-item .swiper-image {
  width: 100%;
  height: 100%;
  display: block;
}
.museum-container .hot-museum-swiper .swiper-item .swiper-info {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 20rpx;
  background: linear-gradient(to top, rgba(0, 0, 0, 0.7), transparent);
  color: #fff;
}
.museum-container .hot-museum-swiper .swiper-item .swiper-info .museum-name {
  font-size: 36rpx;
  font-weight: bold;
  display: block;
}
.museum-container .hot-museum-swiper .swiper-item .swiper-info .museum-desc {
  font-size: 28rpx;
  display: block;
  margin-top: 10rpx;
}
.museum-container .section-title {
  padding: 30rpx;
  position: relative;
  font-size: 36rpx;
  font-weight: bold;
  color: #000000;
  border-top: 1rpx solid #eee;
}
.museum-container .museum-list {
  flex: 1;
  background-color: #fff;
  padding: 0 30rpx;
  box-sizing: border-box;
}
.museum-container .museum-list .museum-item {
  position: relative;
  height: 300rpx;
  margin-bottom: 30rpx;
  border-radius: 10rpx;
  overflow: hidden;
}
.museum-container .museum-list .museum-item .museum-content {
  width: calc(100% + 120rpx);
  /* 包含预约按钮的宽度 */
  height: 100%;
  transition: -webkit-transform 0.3s;
  transition: transform 0.3s;
  transition: transform 0.3s, -webkit-transform 0.3s;
}
.museum-container .museum-list .museum-item .museum-content .museum-name-overlay {
  position: absolute;
  top: 20rpx;
  left: 20rpx;
  color: #ffffff;
  padding: 10rpx 20rpx;
  border-radius: 8rpx;
  font-size: 28rpx;
  font-weight: bold;
  z-index: 2;
}
.museum-container .museum-list .museum-item .museum-content .museum-image {
  width: calc(100% - 120rpx);
  /* 减去预约按钮的宽度 */
  height: 100%;
  display: block;
  border-radius: 10rpx;
}
.museum-container .museum-list .museum-item .reserve-btn {
  position: absolute;
  top: 0;
  right: 0;
  width: 220rpx;
  height: 100%;
  background-color: #9c9c9c;
  color: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: bold;
  transition: opacity 0.3s;
  border-radius: 0 10rpx 10rpx 0;
  z-index: 2;
}
